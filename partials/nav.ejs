<% include ../partials/header.html %>
<!DOCTYPE html>
<html>
  <head>
  <% if (typeof title != "undefined" && title != null) { %>
    <title>
      <%= title  %></title>
  <% } %>
  <style>

    body{
        padding:15px;
    }
    .row{
        margin:15px;
    }
</style>
  </head>
  <body>
   <div>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
     <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/user-list">User List</a>
        </li>

        <% if (typeof localStorage.getItem('loginUser') != "undefined" && localStorage.getItem('loginUser') != null) { %>
        <li class="nav-item dropdown-toggle" data-toggle="dropdown" style="right: 30px; position:absolute;">
            <a class="nav-link" ><%= localStorage.getItem('loginUser') %><i class="fa fa-caret-down" style="padding-left:10px"></i></a>
            <ul class="dropdown-menu">
              <li><a href="logout" class="dropdown-item" >Logout</a></li>
            </ul>
          </li>
          <% } %>
      </ul>
    </div>
  </nav>
  <% include ../partials/footer.html %>
